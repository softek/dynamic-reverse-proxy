start on startup
stop on shutdown

# Automatically restart this service up to 10 times within 5 seconds.
# If the service fails more than 10 times in 5 seconds, stop the job and mark it as failed.
respawn
respawn limit 10 5

chdir /home/softekadmin/applications/reverse-proxy
exec node index.js >> /var/log/illuminate-reverse-proxy.log 2>&1
